var express = require('express');
var router = express.Router();
const gameController = require('../controllers/game.controller');

//router.get('/startgame', gameController.makeGame);

module.exports = router;